
# Admin API bearer for /api/admin/* (beta approvals); unset = admin API disabled
ADMIN_API_TOKEN=

# 32-byte urlsafe-base64 key; synced chats are AES-256-GCM sealed at rest.
# Generate: python -c "import base64,os;print(base64.urlsafe_b64encode(os.urandom(32)).decode())"
SYNC_ENC_KEY=
